# 部署说明（游戏账号管理器）

本工具是一个纯前端单文件 HTML 应用，账号数据保存在浏览器本地（localStorage），不会上传到任何服务器。

## 一、手动部署到 GitHub Pages（只需做一次）

前提：仓库为 **Public（公开）**，且 `index.html` 已位于 `main` 分支根目录。

1. 打开仓库页面 https://github.com/nj186/game-account-manager
2. 点仓库顶部的 **Settings（设置）**
3. 左侧菜单 **Code and automation（代码与自动化）** → 点 **Pages**
4. **Build and deployment（构建与部署）** 区域：
   - **Source（来源）** 选 `Deploy from a branch`（从分支部署）
   - **Branch（分支）** 选 `main`，右侧目录选 `/ (root)`（根目录）
5. 点 **Save（保存）**
6. 等待 1–2 分钟，浏览器访问 https://nj186.github.io/game-account-manager/ 即可使用

## 二、日后更新代码

只要把新 `index.html` 推送到 `main` 分支，GitHub 会自动重新部署，无需再动 Pages 设置：

- **网页方式**：仓库首页点 **Add file（添加文件）** → **Upload files（上传文件）** → 把新 `index.html` 拖进去 → 底部 **Commit changes（提交更改）**
- **本地 Git**：改完后 `git add index.html && git commit -m "更新" && git push`

## 三、数据安全说明

- 账号数据只存在你自己的浏览器本地，不会上传 GitHub，因此公开仓库也不会泄露账号信息
- 换浏览器、清缓存或用无痕模式会丢失数据，请用工具内的「导入 / 导出 JSON」功能定期备份
- 在线版与本地双击打开的 `index.html` 是同一套代码，数据各自独立（不同浏览器/设备的 localStorage 不互通）
